import React from 'react';

/** When true, themed bodies skip SVG foot shadows/highlights (battle uses AnimatedMonster ground shadow). */
export const BattleShadingContext = React.createContext(false);

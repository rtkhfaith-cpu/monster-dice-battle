# Amplify Console → Environment variables (or local .env for dev builds)
# Must be set before `npm run build` — value is inlined via import.meta.env.VITE_SOCKET_SERVER_URL
VITE_SOCKET_SERVER_URL=https://your-socket-server.example.com
